version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --prefer-offline --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
